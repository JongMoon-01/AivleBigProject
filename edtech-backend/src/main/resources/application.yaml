spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/edtech
    username: root 
    password: aivle0517
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect

server:
  port: 8080

jwt:
  secret: aVeryLongAndSecureSecretKeyForJWTTokenGenerationThatShouldBeAtLeast256BitsLong
  expiration: 3600000

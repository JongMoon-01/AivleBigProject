spring:
  datasource:
    url: jdbc:mysql://localhost:3306/edtech?useSSL=false&serverTimezone=UTC
    username: root
    password: '!ehdgml0315'
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect  # ← MySQL용 방언으로 변경
  web:
    resources:
      static-locations: classpath:/static/

server:
  port: 8080

jwt:
  secret: your_jwt_secret_key_here
  expiration: 3600000


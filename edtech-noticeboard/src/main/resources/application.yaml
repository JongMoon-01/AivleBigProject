server:
  port: 8083
  error:
    include-message: always
    include-binding-errors: always

app:
  upload-dir: C:/tmp/uploads

backend:
  base-url: http://localhost:8080  # 메인 백엔드 주소

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/noticeboard?allowPublicKeyRetrieval=true&useSSL=false
    username: aivle17
    password: aivle0517
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
  web:
    resources:
      static-locations: classpath:/static/

jwt:
  secret: "87YlPD4ykG0WHHB6s2JKGSWLYQDVCIzT8YebMT0Z1EFpYXhAsZo6RyAa88KeRuWddvp1vIXihU3X9oPxVXuhsw=="

logging:
  level:
    org.springframework.security: DEBUG
